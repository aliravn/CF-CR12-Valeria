SELECT * 
FROM `posts` 
JOIN `locations` ON `fk_location` = `locationID` 
WHERE `poi_type` = 'event';


SELECT posts.*, concat(address, ' ', city, ' ', zipcode, ' ', country) as location 
FROM `posts` 
JOIN `locations` ON `fk_location` = `locationID` 
WHERE `poi_type` = 'event';
